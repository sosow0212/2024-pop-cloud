/**
 * 1. 메인 페이지 진입 시 유저 위치 확인 client 어디에서 다룰지
 */
import { Map } from "@/components/map";

const MainPage = () => {
  return <div className="relative h-full bg-green-300"></div>;
};

export default MainPage;
