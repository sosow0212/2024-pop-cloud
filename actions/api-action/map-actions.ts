const getMarkers = async (props: kakao.maps.LatLngBounds) => {
  // try {
  //   const url = `${}`
  //   const res = await fetch(url);
  //   if (res.ok) {
  //     const data = await res.json();
  //     return data;
  //   }
  // } catch (error) {
  //   console.log(error);
  // }
};

export { getMarkers };
